---
description: "Git 工作流——分支策略、提交约定、防御性提交、PR 模板。进行 Git 操作时应用。"
alwaysApply: false
globs:
  - ".gitignore"
  - ".husky/**"
---

# Git 工作流

## 主干开发模式

- 主分支：`main`
- 功能分支：`feat/<功能名>`
- 修复分支：`fix/<问题描述>`
- 短命分支（1-2 天内合并）

### 分支流程

```
main ──────●──────●──────●──────●──────
            \           /
feat/auth    ●────●────●
```

1. 从 `main` 创建分支
2. 按 TDD 进行原子提交
3. PR 前在 `main` 上 rebase
4. Squash merge 合入 `main`
5. 合并后删除分支

## 提交约定

### 格式

```
<类型>(<范围>): <主题>

[可选正文]

[可选脚注]
```

### 类型

| 类型 | 用途 |
|------|------|
| `feat` | 新功能 |
| `fix` | Bug 修复 |
| `docs` | 仅文档变更 |
| `test` | 添加/更新测试 |
| `refactor` | 既不修复也不新增的代码变更 |
| `perf` | 性能优化 |
| `chore` | 工具链、依赖、CI 变更 |
| `style` | 格式调整，不改代码逻辑 |

### 规则

- 主题：祈使语气、小写、不加句号、最多 72 字符
- 正文：解释**为什么**而非做了什么（diff 能看到做了什么）
- 每次提交只含一个逻辑变更
- TDD 循环中每次绿灯后立即提交
- 禁止向 main 提交无法运行的代码

## 防御性提交

- 任何多文件重构前，确保已有的工作变更已提交
- 完成一个逻辑工作单元后，建议创建检查点提交
- 永远不要将无关变更合并到同一个 commit 中

## 提交前检查清单

每次提交前确认：

- [ ] `pnpm typecheck` 通过
- [ ] `pnpm lint` 通过
- [ ] `pnpm test` 通过
- [ ] 暂存文件中没有密钥
- [ ] 提交信息符合约定

## Pull Request 模板

```markdown
## 概述
[1-3 个要点描述变更内容]

## 规范引用
[链接到 openspec/changes/<name>/specs/ 中的规范文件]

## 变更内容
- [文件级别的变更说明]

## 测试计划
- [ ] 已添加/更新单元测试
- [ ] 已添加/更新集成测试
- [ ] 已添加/更新 E2E 测试（如涉及 UI）
- [ ] 已完成手动测试

## 检查清单
- [ ] TypeScript 严格模式，无 `any`
- [ ] 所有测试通过
- [ ] Lint 检查干净
- [ ] 规范需求已验证
- [ ] 没有遗留 TODO/FIXME
```
