---
description: "安全标准——OWASP Top 10、密钥管理、输入验证、认证授权。全局应用以防止漏洞。"
alwaysApply: true
---

# 安全标准

## OWASP Top 10

1. **输入验证** — 用 Zod 等工具验证所有外部输入
2. **输出编码** — 渲染前转义（防 XSS）
3. **认证** — 使用成熟的认证库（better-auth、next-auth 等）
4. **授权** — 每个受保护操作都检查权限
5. **密钥管理** — 禁止硬编码；使用环境变量
6. **依赖审计** — 定期运行 `pnpm audit`
7. **SQL 注入** — 只使用参数化查询
8. **CORS** — 明确配置，生产环境禁止 `*`
9. **限流** — 保护所有公开端点
10. **日志** — 禁止记录密码、令牌或个人信息

## 密钥管理

- 绝对不要在代码中硬编码密钥、API Key、Token、密码或凭证
- 绝对不要在日志中记录密码、Token、原始密钥或敏感用户数据
- 绝对不要提交 `.env`、`credentials.json`、`token.json` 或私钥文件
- 所有敏感值使用环境变量或密钥管理服务
- 任何已提交的密钥视为已泄露，必须立即轮换

## 输入验证

- 在每个边界验证并清理所有用户输入
- 将上传、URL、查询参数、请求头和请求体视为不可信
- 输入验证使用白名单而非黑名单
- 客户端和服务端都要验证——客户端验证是用户体验，服务端验证是安全保障

## 认证与授权

- 每个受保护端点都必须执行认证检查
- 使用基于会话或 JWT 的认证，并设置正确的过期时间
- 在认证端点上实施速率限制
- 永远不要存储明文密码。使用 bcrypt/argon2 加正确的盐值

## 数据保护

- 清理所有输出以防止 XSS 攻击
- 使用参数化查询防止 SQL 注入
- 设置正确的 CORS、CSP 和安全响应头
- 全站使用 HTTPS。禁止混合内容
- 永远不要向终端用户返回内部错误详情（堆栈跟踪、数据库错误）

## 环境与配置

- 所有配置来自环境变量或配置文件
- 在启动时验证配置，缺少值时快速失败
- 维护 `.env.example`，记录所有必需变量（不包含真实值）
