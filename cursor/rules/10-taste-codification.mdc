---
description: "偏好持续捕获——当用户反馈设计偏好时，自动提取规则并持久化到规则文件。"
alwaysApply: true
---

# 偏好持续捕获（Taste Codification）

当用户给出 UI、措辞、布局或任何设计决策的反馈时，将其视为偏好信号。

## 工作流程

1. **识别偏好信号**：用户对代码风格、UI 设计、命名方式、交互模式等给出反馈或纠正
2. **提取规则**：将反馈提炼为可复用、可执行的规则
3. **持久化**：更新相关规则文件（`.cursor/rules/` 中最相关的文件），使后续会话自动遵循
4. **目标**：逐步积累用户偏好，使未来会话无需重复纠正即可产出正确结果

## 捕获范围

- UI 组件样式偏好（圆角/颜色/间距/布局模式）
- 代码风格偏好（命名、缩进、组织结构）
- 交互模式偏好（toast vs dialog、乐观更新 vs 等待响应）
- 文案措辞偏好（正式/轻松、中文/英文、术语选择）
- 架构决策偏好（组件拆分粒度、状态管理方案）

## 规则

- 用户纠正同一问题两次时，必须创建或更新规则，不要等第三次
- 新规则添加到最相关的作用域文件中（前端规则放 `03-frontend.mdc`，安全规则放 `06-security.mdc`）
- 每条规则必须具体可执行，禁止模糊描述（例：✅ "按钮圆角使用 rounded-lg" ❌ "按钮要好看"）
- 规则文件是版本控制的资产——像对待生产代码一样对待规则的变更

## 修正循环熔断器

- 如果连续 3 次修正同一类问题，停下来分析根因
- 可能原因：规则冲突、规则过于模糊、规则被上下文挤出
- 解决方案：
  - 规则冲突 → 删除或合并冲突规则
  - 规则模糊 → 增加具体示例
  - 规则丢失 → 将规则迁移到 alwaysApply 文件或提升优先级
