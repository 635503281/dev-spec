---
description: "工程流程——TDD、验证、调试、代码审查。与语言无关的通用工程纪律。"
alwaysApply: true
---

# 工程流程

与编程语言无关的通用工程纪律。适用于所有编码任务。

## 五条铁律

1. **先写规范再写代码** — 先明确要构建什么，再动手
2. **先写测试再写实现** — RED → GREEN → REFACTOR（TDD）
3. **先拿证据再下结论** — 运行验证命令、展示输出，然后才能声称成功
4. **先找根因再修复** — 系统化排查，绝不盲猜
5. **先理解再删除** — 切斯特顿栅栏原则适用于所有现有代码

---

## TDD 工作流

```
没有失败的测试，就不写生产代码
```

先写了代码再补测试？删掉。重头开始。没有例外。

### RED-GREEN-REFACTOR 循环

**1. RED — 编写失败的测试**

```typescript
test('当 [条件] 时应该 [预期行为]', () => {
  const result = functionUnderTest(input);
  expect(result).toBe(expectedOutput);
});
```

要求：
- 每个测试只测一个行为
- 测试名称清晰描述意图
- 使用真实代码，不用 mock（除非不可避免）

**2. 验证 RED — 看它失败**

运行：`pnpm test <路径>`

确认：
- 测试确实失败（而非报错）
- 失败信息符合预期
- 失败原因是功能缺失

**3. GREEN — 最小实现**

写出能让测试通过的**最简单**代码。不多不少。

**4. 验证 GREEN — 看它通过**

运行：`pnpm test <路径>`

确认：
- 目标测试通过
- 所有其他测试仍然通过
- 输出中没有警告或错误

**5. REFACTOR — 清理**

只在绿灯之后：
- 消除重复
- 改善命名
- 提取辅助函数
- 全程保持测试绿灯

**6. COMMIT — 提交**

```bash
git add -A
git commit -m "feat: [具体描述]"
```

### 测试质量检查清单

- [ ] 每个新函数都有对应测试
- [ ] 每个测试都先看到失败
- [ ] 每个测试失败的原因都是预期内的
- [ ] 写了最小化的实现代码
- [ ] 所有测试通过
- [ ] 覆盖了边界情况和错误路径
- [ ] 没有使用 mock（除非绝对必要）

### 测试文件约定

- 就近放置：`src/lib/auth.ts` → `src/lib/auth.test.ts`
- 或镜像目录：`src/lib/auth.ts` → `tests/unit/lib/auth.test.ts`
- E2E 测试：`tests/e2e/<功能名>.spec.ts`
- 命名：`describe('[模块名]', () => { test('should ...') })`
- 金字塔：**80%** 单元 / **15%** 集成 / **5%** E2E

---

## 质量门禁（不可妥协）

在声称任何工作已完成之前：

1. 所有测试通过（展示输出）
2. Lint 检查通过（展示输出）
3. 类型检查通过（展示输出）
4. 逐行检查规范需求是否满足
5. 没有 `TODO`、`FIXME` 或占位代码

```bash
pnpm typecheck        # TypeScript 类型检查
pnpm lint             # ESLint 检查
pnpm test             # 单元/集成测试
pnpm build            # 生产构建
```

涉及 UI 变更时，额外运行：

```bash
pnpm test:e2e         # Playwright E2E 测试
```

展示输出。然后做出声明。

### 验证门控协议

在声称任何状态之前必须执行：

1. **识别**：用什么命令能证明这个声明？
2. **运行**：执行完整命令（重新运行，不用缓存）
3. **阅读**：完整输出、检查退出码、统计失败数
4. **核实**：输出是否支持你的声明？
   - 否 → 如实陈述实际状态并附带证据
   - 是 → 陈述结论并附带证据
5. **然后**：才能做出声明

### 验证对照表

| 声明 | 必须有 | 不够的 |
|------|--------|--------|
| "测试通过" | 测试输出：0 个失败 | 之前的运行结果、"应该能过" |
| "Lint 干净" | Lint 输出：0 个错误 | 部分检查 |
| "构建成功" | 构建输出：退出码 0 | "Lint 过了" |
| "Bug 已修复" | RED-GREEN 循环已验证 | "代码改了" |
| "需求已满足" | 逐行检查清单 | "测试过了" |

### 危险信号 — 立即停下

回复中出现以下词语而没有新鲜证据 = 违规：

- "应该"、"大概"、"看起来"
- "太好了！"、"完美！"、"搞定！"
- "我有信心"
- "看起来是对的"

---

## 系统化调试

```
没有根因调查，就不做修复
```

如果没有完成第一阶段，就不能提出修复方案。

### 第一阶段：根因调查

在尝试任何修复之前：

1. **仔细阅读错误信息** — 完整的堆栈跟踪、行号、错误代码。不要跳过警告。
2. **稳定复现** — 触发问题的精确步骤。是否每次都能复现？无法复现 → 收集更多数据，不要猜测。
3. **检查最近变更** — `git diff` 和近期提交。新依赖或配置变更。环境差异。
4. **追踪数据流** — 错误值从哪里产生？向上游追溯直到找到源头。在源头修复，而非在症状处打补丁。
5. **多组件系统中收集证据** — 在每个组件边界记录输入/输出。运行一次找到具体断裂点。

### 第二阶段：模式分析

1. 在代码库中找到类似的**正常工作**的代码
2. 对比正常 vs 异常 — 列出所有差异
3. 理解依赖关系和隐含假设

### 第三阶段：假设与测试

1. 形成一个假设："X 是根因，因为 Y"
2. 做最小的改动来验证假设
3. 一次只改一个变量
4. 假设被验证 → 进入第四阶段
5. 假设错误 → 形成新假设（不要叠加修复）

### 第四阶段：TDD 修复

1. 编写一个能复现 bug 的失败测试
2. 实施修复（只改一处）
3. 验证测试通过
4. 验证没有回归（运行完整测试套件）

### 如果已失败 3 次以上

**停下来。** 这说明是架构问题，不是表面 bug。

- 质疑根本方向 — 这个模式本身是否合理？
- 在继续尝试之前与用户讨论
- 考虑重构而非打补丁

### 调试中的危险信号

如果你发现自己在想以下内容，**立即停下，回到第一阶段**：

- "先快速修一下"
- "试着改一下 X 看看"
- "我有信心这次能行"
- "再试一次"

---

## 代码审查五轴

审查代码时检查：
1. **正确性** — 是否符合规范要求？
2. **安全性** — 有没有漏洞？
3. **性能** — 有没有明显瓶颈？（N+1、无界循环、缺缓存）
4. **可读性** — 新人能看懂吗？
5. **可维护性** — 改动时会不会牵一发动全身？

---

## Agent 行为纪律

### 反幻觉规则

- 导入任何包之前，先验证其存在于 `package.json` 中或运行 `pnpm list <pkg>`
- 使用任何 API 或方法之前，先验证其存在于代码库或官方文档中
- 如果不确定某个库的 API，明确说明不确定并询问——不要猜测
- 永远不要凭空捏造函数签名、配置选项或 CLI 参数

### 零占位符策略

- 永远不要输出 `// TODO: implement`、`// rest of code`、`// ...existing code` 或 `/* add more here */`
- 每个函数、组件和模块都必须完整实现
- 如果某个函数无法完成，解释原因并请求指导
- 不完整的实现比没有实现更糟糕

### Agent 安全边界

- 绝对不要在没有明确确认的情况下删除配置文件（.env、package.json、tsconfig）
- 绝对不要在没有人工批准的情况下执行破坏性命令（rm -rf、DROP TABLE）
- 绝对不要在没有人工审查的情况下自动 commit 或 push 到远程仓库
- 如果发现安全漏洞，立即停止并报告

### 技能调用规则

> 继承自 Superpowers:using-superpowers

**哪怕只有 1% 的可能性某个技能适用，就必须调用它。**

收到用户消息时：
1. 先由编排引擎判断阶段
2. 检查该阶段是否有适用的 Superpowers 技能
3. 如果有 → 宣布"正在使用 [技能名] 来 [目的]"→ 严格遵循技能流程
4. 如果技能有检查清单 → 创建 TodoWrite 逐项追踪

---

## 反合理化

以下想法都是错误的 — 必须立即纠正：

| 借口 | 真相 |
|------|------|
| "太简单了，不需要规范" | 简单的工作写简单的规范。写。 |
| "太简单了，不需要走流程" | 编排引擎自动判断。你不需要判断。 |
| "太简单了，不需要测试" | 30 秒就能写。动手。 |
| "我之后再加测试" | 事后写的测试立即通过，什么都证明不了。先写测试。 |
| "已经手动测过了" | 手动 ≠ 系统化。没有回归保障。 |
| "TDD 太慢了" | 调试花的时间更多。 |
| "现在应该可以了" | 运行命令。看输出。 |
| "先快速修一下，之后再查原因" | 快速修复只会制造更多 bug。找根因。 |
| "这个不需要技能" | 如果技能存在，就用它。 |
| "我先探索一下代码" | 技能会告诉你怎么探索。先检查技能。 |
| "再试一次" | 停下。回到根因调查。 |
