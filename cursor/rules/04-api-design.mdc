---
description: "API 设计——契约优先、REST 约定、响应结构、输入验证。设计或编写 API 时应用。"
alwaysApply: false
globs:
  - "src/services/**/*.ts"
  - "src/api/**/*.ts"
  - "**/route.ts"
  - "**/actions.ts"
---

# API 设计

## 设计原则

- **契约优先**：先定义 TypeScript 接口 → 针对接口写测试 → 然后实现
- **海勒姆定律**：记录所有行为，不仅仅是预期行为。考虑任何变更的影响。
- **单版本规则**：同时只保留一个 API 活跃版本，始终提供迁移路径

## 服务层模板

```typescript
export interface UserService {
  getUser(id: string): Promise<Result<User, UserNotFoundError>>;
  createUser(input: CreateUserInput): Promise<Result<User, ValidationError>>;
  updateUser(id: string, input: UpdateUserInput): Promise<Result<User, UserNotFoundError | ValidationError>>;
  deleteUser(id: string): Promise<Result<void, UserNotFoundError>>;
}

export interface CreateUserInput {
  email: string;
  name: string;
}

export class UserNotFoundError extends Error {
  constructor(public readonly userId: string) {
    super(`用户未找到: ${userId}`);
    this.name = 'UserNotFoundError';
  }
}
```

## REST API 约定

```
GET    /api/v1/users          → 列出用户
GET    /api/v1/users/:id      → 获取单个用户
POST   /api/v1/users          → 创建用户
PATCH  /api/v1/users/:id      → 更新用户
DELETE /api/v1/users/:id      → 删除用户
```

## 统一响应结构

```typescript
// 成功
{ data: T, meta?: { page, total, ... } }

// 错误
{ error: { code: string, message: string, details?: unknown } }
```

## HTTP 状态码

| 状态码 | 用途 |
|--------|------|
| 200 | 成功 |
| 201 | 已创建 |
| 204 | 无内容（删除） |
| 400 | 请求错误（验证失败） |
| 401 | 未认证 |
| 403 | 无权限 |
| 404 | 未找到 |
| 409 | 冲突 |
| 422 | 无法处理的实体 |
| 429 | 请求频率受限 |
| 500 | 服务器内部错误 |

## 输入验证

使用 Zod 对所有外部输入进行运行时验证：

```typescript
import { z } from 'zod';

export const CreateUserSchema = z.object({
  email: z.string().email(),
  name: z.string().min(1).max(100),
});

export type CreateUserInput = z.infer<typeof CreateUserSchema>;
```
